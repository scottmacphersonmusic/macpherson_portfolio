<h1>Transcriptions</h1>
<ul>
  <% @transcriptions.each do |trans| %>
    <em>
      <%= link_to(trans.song_title, trans) %>
    </em> ~ <%= trans.soloist_last_name %>, <%= trans.soloist_first_name %>
  <% end %>
</ul>
