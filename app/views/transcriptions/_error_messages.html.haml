- if @transcription.errors.any?
  .row
    .large-12.columns
    The form contains #{pluralize(@transcription.errors.count, 'error')}:
    %ul
      - @transcription.errors.full_messages. each do |msg|
        %li= msg
