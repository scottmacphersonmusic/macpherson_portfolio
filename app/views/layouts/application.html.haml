!!! 5
%html{lang: 'en'}
  %head
    %meta{charset: 'utf-8'}
    %meta{name: 'viewport', content: 'width=device-width, initial-scale=1.0'}

    %title= content_for?(:title) ? yield(:title) : 'Scott Macpherson'

    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags

  %body
    .top-bar
      .top-bar-left
        %ul.dropdown.menu
          %li.menu-text= link_to 'Scott Macpherson', root_path
          %li= link_to 'transcription', transcriptions_path

    - flash.each do |key, message|
      .alert= message

    = yield
