- flashes = {success: 'success', error: 'alert'}
- flashes.each do |key, value|
  - if flash[key]
    .alert-box{class: value, data: { alert: '' }}
      = flash[key]
      = link_to '&times;'.html_safe, '#', id: 'close'
